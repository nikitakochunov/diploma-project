import districts from './fake.api/districts.api'
import metroStations from './fake.api/metroStations.api'
import ads from './fake.api/ads.api'

const api = {
  districts,
  metroStations,
  ads,
}

export default api
